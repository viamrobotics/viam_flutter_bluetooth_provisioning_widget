# Copy this file to .env and fill in your values.
#   cp .env.example .env

# Viam API credentials (required)
API_KEY_ID=your-api-key-id
API_KEY=your-api-key
ORG_ID=your-org-id
LOCATION_ID=your-location-id

# Wi-Fi credentials — used by the integration test to provision the device (required for tests)
WIFI_SSID=your-wifi-network
WIFI_PASSWORD=your-wifi-password

# Optional: Bluetooth pre-shared key (defaults to 'viamsetup' if omitted)
# PSK=viamsetup
