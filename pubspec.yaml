name: viam_flutter_bluetooth_provisioning_widget
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: '>=3.6.1 <4.0.0'
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

  permission_handler: ^12.0.0+1
  viam_sdk: ^0.6.4
  collection: ^1.18.0
  viam_flutter_provisioning: ^0.0.10
  provider: ^6.1.5
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0